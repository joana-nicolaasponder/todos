export interface Tasks {
  id: number
  taskDetails: string
  priority: string
  completed: boolean
}

export interface TaskData {
  taskDetails: string
  completed: boolean
}

export interface Task {
  taskDetails: string
}
